// Package ops applies the recording pipeline: owner exclusion, the collection switch, identity
// derivation, bot classification, then the write — in that order, once, for both the browser
// beacon and every server-side emit.
package ops
