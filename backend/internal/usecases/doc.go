// Package usecases —— 过渡态:通用 application 层已基本解散(capreg glue→capabilities/capload,
// 单域用例→各自 domain)。此处仅剩 summarize 报告的 styling/sanitize(report_document/
// report_sanitize),它们随 summarize 能力的完整外置一起搬走后,本包即可删除。
package usecases
