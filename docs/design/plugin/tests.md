# Tests — moved

The eiab test map is now one authoritative, behavior-decoupled doc:
**[`everything-is-a-block-tests.md`](./everything-is-a-block-tests.md)**.

It folds in what used to live here: the "safety net vs subtractive-rule" discriminator (the
provider-agnostic net that must stay green through the fold, §D), the microsite-side owed items, and
the two writing traps (absence assertions; a string from two sources). See that doc's *What's left*
and *Two traps* sections.
