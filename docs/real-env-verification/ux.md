# UX findings (continuous)

Judged live while driving the real UI (headed) during verification. Separate from functional Findings (`findings.md`) — this is about UI/UX quality: clarity, friction, feedback, affordances, aesthetics, error copy, responsiveness.

Severity: 🔴 broken/blocking · 🟠 friction/confusing · 🟡 polish · 💡 idea

| ID | Surface | Observation | Sev |
|----|---------|-------------|-----|
| UX-78 | `/wiki/<path>` (corpus reader, first screen) | **The first screen says the same three things twice.** On a real note (`simon-ando`, 2026-08-14, `corpus-render/shots/cr-13`): the **title** is printed twice — ~64px inside the hero band, then again ~46px as the article heading, ~90px apart, with nothing between them to make the second a different statement. The **date** is printed three times (breadcrumb right edge, hero-band footer, meta row). The **source count** twice, in two vocabularies — `0 sources cited` in the breadcrumb line and `0 CORPUS SOURCES` in the meta row. And the hero band itself is **~400px of generated gradient** carrying only those repeats, because this note has no cover image: on a note whose value is dense math, the first screen is mostly empty wash and the content starts below the fold. With a cover image that panel earns its height; with none it should collapse. Pure presentation — no new data, nothing to decide, no test changes beyond testids. | 🟠 recorded |
| UX-1 | all pages | `GET /favicon.ico` → 404; no favicon → broken/blank tab icon, looks unfinished. | ✅ ce91697 (favicon.ico+icon.svg) |
| UX-2 | login → admin | After sign-in the owner lands on `/admin/page` (Public Face editor), not a dashboard/overview. Odd default for a returning owner. | ✅ b6c30d3 (→dashboard) |
| UX-3 | /login | "WHAT YOU GET" marketing column (01–04) shown on the **owner's own** login — they already deployed the instance; the pitch is redundant here. | ✅ b6c30d3 (offers off on /login) |
| UX-4 | admin shell | `ZodError` + a broken "requests" sidebar badge on every admin load (see F-C-1) — visible console noise + a nav badge that never renders a count. | ✅ (F-C-1 fixed) |
| UX-16 | /admin/dashboard (corpus-pulse) | The corpus-pulse sparkline draws the **real** series (rot-A1) but is **illegible**: no y-axis, no gridline, no per-point marker, **no hover tooltip showing date+count**. Owner sees two triangles, can't read which day or how many (real 07-13=219 / 07-16=188). A growth chart you can't read a number off. Owner-flagged live 2026-07-18: "没有轴，没有 hover tooltip 显示具体数字". → **F-C-5**. `Sparkline.tsx` renders shape-only `<polygon>`+`<polyline>`; shared atom, fix helps every sparkline. | ⬜ recorded |
| UX-17 | admin shell (top-bar badge) | The env/version badge reads a hardcoded **`v0.1 · dev`** (`TopBar.tsx:20`, `buildTag` never passed) — shows `dev` on **prod**, and contradicts the login page's `v1.0.0`. Names-that-lie: an env label tracking nothing. → **F-C-4**. | ⬜ recorded |
| UX-18 | / (public homepage, returning visitor) | On reload of a tab that holds a prior chat's conversation id (sessionStorage), the page fires `GET /api/v1/conversations/{id}` which **401s** (the conversation was invalidated/GC'd server-side) — **twice** (identical double-fetch), both swallowed. UX recovers correctly (identity picker shown), so it's console-noise only, but a returning visitor whose session lapsed sees 2 red 401s and the resume fetch shouldn't fire (or should clear the stale id + start fresh on 401). **Needs clean repro** (a fresh visitor whose conversation is invalidated) — surfaced during sweep from my own prior verify sessions; cleared once sessionStorage was wiped. Low sev. | ⬜ recorded (needs clean repro) |
| UX-19 | /admin/dashboard (jobs card) | The jobs "top match" empty state reads **"register sources to start matching"** even when a source **is** registered (sources page: "1 active" gitlabgreenhouse). With a source present but the 1-day listings pool aged out (0 in pool), the correct next step is **fetch**, not register — the message points at a step already done. Sub-line does show the full "sources → fetch → listings" flow, so low sev, but the headline empty-state verb is wrong for this state. | ⬜ recorded (low) |
| UX-20 | /wiki (invited-visitor reader) | An **invited** visitor (banner "unlocked · SUBJ-V01", whose code grants `wiki://**`) opens /wiki and the reader is **empty**: header "**223 entries · 4 roots · 223 gated**", body "The corpus, by entry" shows **0 items**, sidebar 4 roots unpopulated. Yet the **chat** answered from those same 223 wiki notes (searched 8 · read 11) seconds earlier. **ATTRIBUTION CORRECTED 2026-07-18 (earlier guess was wrong):** the reader does NOT ignore the code ACL. Live-tested both cases — a **fresh** `wiki://**` session returns the 4 roots correctly; the empty view happened because the visitor session had **expired from Redis (60-min TTL)** while the frontend kept showing "unlocked" from stale localStorage → `wiki-tree` fell to anonymous (published-only, 0 published) → empty. The prior "published gate ignores ACL" reading would have driven a wrong fix (the reader already honors the grant). Real bug is the **stale-session-shows-as-unlocked** + the **owner-scoped stats header** (always 223/4 regardless of viewer). Folded into **F-L-11**. | 🟠 recorded (attribution corrected) |
| UX-21 | /writings/&lt;slug&gt; (cover-image title overlay) | The writing's title is rendered **over** the cover image with no contrast protection — on a busy/high-contrast cover (the real `media-verify` cover is black-and-white QR-noise) the serif title "cover image render test" is **illegible** (no scrim, no text-shadow, fixed ink color that vanishes against the pattern). Owner-flagged live 2026-07-18 ("这个字都看不清 … 应该也需要能挑颜色才行"). corpus-media itself **works** (the cover image does render); this is the overlay legibility. Fix: an automatic scrim/gradient behind the title (robust default) and/or an owner-selectable overlay text color — same "make it configurable / legible" theme as F-C-5 (sparkline) and the reader cluster. | ⬜ recorded |
| UX-22 | /wiki/&lt;path&gt; (private entry, invited visitor) | The browser **tab title reads "not found"** on every gated wiki entry an invited visitor opens (`/wiki/cybernetics`, `/wiki/math/analysis/lagrangian`, …) **while the body renders the real content fine.** Cause: Next.js `generateMetadata` for `/wiki/[...path]` fetches the entry **server-side (SSR)**, where the visitor's bearer lives only in localStorage → 404 → title "not found"; the client render then has the bearer and shows the entry. Defensible for SEO (don't emit private titles to crawlers), but a visitor who is *reading the note* sees a tab that says the page doesn't exist — a name-that-lies at the tab level. Consider a neutral private-entry title ("sijie · wiki") instead of "not found". Observed live 2026-07-18. | ⬜ recorded (likely 4b / owner call) |
| UX-23 | /wiki/&lt;path&gt; (reader body) | Obsidian folder-note **metadata leaks into the rendered reader body**: `/wiki/cybernetics/theory` and `/wiki/math/analysis/lagrangian` render a blockquote `> Parent: [[cybernetics]]` / `> Parent: [[math]]` (and `Prereq: [[lagrangian]]`) at the top of the note — structural pointer lines the owner uses for the vault graph, shown verbatim as content. The reader-body sibling of F-R-1/F-R-2 (which stripped this class from *excerpts*); here it's the full reader. Compounded by F-L-12 (the `[[cybernetics]]` in it is also dead literal text). Whether to strip `Parent:`/`Prereq:` structure lines from the reader is a call, but rendering them as a bare blockquote reads as noise. Observed live 2026-07-18. | ⬜ recorded |
| UX-8 | /gate (BYOAI form) | Browser autofill drops the owner's saved **email into the "model" field and password into the "api key" field** (fields lack `autocomplete=off`/`new-password`) — a visitor's own saved creds would autofill the same way; risk of submitting a password as a key. | ✅ b3a57aa |
| UX-9 | visitor chat | Two competing inputs: a "search the corpus…" box at the top and the "ask…" chat dock at the bottom. Which do I use? The search box (see F-A-2) shouldn't be there at all; even setting that aside, two inputs on one surface with different mental models is confusing. | ✅ (F-A-2 removed the search box) |
| UX-7 | /wiki (public reader tree) | ~~advertises "33 entries · 24 roots · 32 gated" but renders a single flat item~~ **Partly fixed:** F-L-2 heal landed — sidebar is now a real **tree with 5 roots** (cybernetics/math/optimization/orbit/software, expand arrows), header "50 entries · 5 roots · 49 gated". **Residual (still 🟠):** the main reader body ("The corpus, by entry") still renders **only 1 item (orbit)** — the sole published node — so the header-promises-50 / body-shows-1 mismatch persists. **Sub-flag checked & CLEARED:** anonymous `GET /api/v1/wiki-tree` returns **only `orbit`** (the 1 public node) — private titles do NOT leak; the full 5-root tree only renders because I viewed it owner-authed (correct ACL, owner previews all). So the residual is purely presentational: header says "50 entries" while the anonymous body shows 1. | 🟠 |
| UX-6 | /admin/wiki (corpus views) | Corpus is inherently a **tree** (folder-notes, `Parent: [[..]]` parent-child, nested `wiki/math/`, `wiki/cybernetics/theory/`) but the admin renders it as a **flat card grid + tag chips**. On the real 190-note nested vault: hierarchy is invisible, no tree nav / collapse-expand, 190 flat cards is unwieldy, and card previews spill raw structure lines (`# X > Parent: [[corpus]]`). Loses the whole navigable structure Obsidian has. (Roadmap 1b graph likely addresses, but today it's flat + ugly at real scale.) | ✅ d256f95 (tree⇄grid toggle) |
| UX-5 | /admin/obsidian | Whole section reads as operational — a vault path, mode/notes/size/last-sync stats, "import/export" action buttons, a "recent events" log — but it's an **inert mockup** (buttons dead, stats fake). Owner will click "import vault zip", nothing happens, no feedback. The real import is unlabelled-as-such in the *writings* section. Deeply misleading. | ✅ (F-L-1 fixed) |
| UX-10 | visitor chat (tool-call progress) | On a real stance question DeepSeek made **11+ `corpus_search`/`corpus_read` calls in one turn**, and each renders as its **own tall, near-empty sandbox-iframe card stacked vertically in the transcript** → a long sparse column of "searched · N entries" / "browsed · N entries" blocks with big empty gaps, dominating the conversation and shoving the actual answer far below the fold. Intended behavior: the progress/throbber should **hold a single fixed spot (top-left of the dialog) and update in place**, not spawn one flashing stacked card per tool call. The per-tool `ui://` card pattern doesn't scale past a couple of calls. | ✅ a52985c |
| UX-11 | /admin/connectors (booking policy) | The Google-Calendar Booking-policy **timezone dropdown defaults to `-11:00 American Samoa Time – Midway`** (the first offset in the list) for an owner clearly not there. If unnoticed, every bookable slot is computed in the wrong TZ → visitors get offered/booked at times ~19h off. Should default to the owner's detected TZ (or at least UTC), not the list head. | ✅ b3a57aa |
| UX-14 | /admin/agent-skills (marketplace sources) | The source tabs are **all / github / skillsmp**, but SkillsMP is fiction (`api.skillsmp.com` doesn't resolve → 0 results), so **SKILLSMP is a dead tab** (click → empty) and **ALL ≡ GITHUB** (both 12) — "ALL" is supposed to be the union of the sources, but with one real source + one dead one it's redundant and misleading. Same dead-affordance class as F-E-1 / F-L-1. Fix folds into the marketplace rework: drop SkillsMP, add a real community-GitHub source (`topic:claude-skill`), then ALL = official ∪ community becomes a meaningful union. | ✅ d635d4e (SkillsMP real) |
| UX-13 | /admin/agent-skills (marketplace) | The GitHub marketplace loads real `anthropics/skills` entries (good — §F now works), but every card renders an **empty description**, a **blank version** (`anthropics · v` with nothing after `v`), and **★ 0** for all. The owner picks "install" with no idea what the skill does or its version/popularity — the SKILL.md frontmatter description isn't surfaced in the list. Makes the marketplace a blind-install. | 🟠 **RE-OPENED 2026-07-15** — descriptions ✅ (real SKILL.md text now shows), but **version is still blank** ("anthropics · v" with nothing after `v`, all cards) and **★ 0 for every card** — the 9d82762 fix populated descriptions but not version/stars. Plus one card ("Claude Api") shows a raw **`|-`** (a leaked YAML block-scalar — its description didn't parse). "Marked fixed" ≠ fixed live. **RE-CONFIRMED live 2026-07-18** (now under the merged /admin/skills → marketplace tab): all cards still `anthropics · v` (blank version) + **★ 0**, and "Claude Api" still renders `|-`. Still 🟠. |
| UX-12 | admin section headings | Section `<h1>`s concatenate the title and the mono subtitle with **no separating space** — `connectors· calendar · mail live…`, `wiki· 50 entries`. A missing space before the leading `·` on (at least) connectors + every corpus genre header. Pure polish, but on every admin page. | ✅ b3a57aa |
| UX-15 | /admin/connectors (add-connector catalog) | Three ✉ "communication" cards read as duplicates — **email** · **smtp** (BUILT-IN) · **mail** — same icon, all "mail". Per `connector-registry.ts` they conflate two axes: `email` (form `smtp_host/smtp_user/smtp_pass`, blurb "ping you on a redaction" = *inbound notify*) and `smtp` (form `host/port/username/password/from/tls` = *the transport*) are **both bare-SMTP credential connectors** — the owner would enter SMTP creds twice for two look-alike cards; `mail` (assemble) is the *outbound* "confirmations in your name" umbrella that itself resolves to SMTP-or-SaaS. So setting up mail presents three overlapping ways in with no clear difference; the roles (notify / send-as-me / transport) aren't conveyed by label or ✉ icon. Suggest: one mail-transport config (fold `email` into `smtp`), make the redaction-ping a *setting* on the mail connector rather than a peer card, and label the remaining cards by role. Owner-spotted, 2026-07-15. **Iron-rule-4b: no test — naming/redundancy is a taste call; owner decides.** | 🟠 |
| UX-16 | visitor chat (booked card) | When the visitor asks to book **and** send a confirmation in one turn, the agent auto-calls `send_confirmation` (email really goes out), yet the booked card still renders a live-looking **"Send a confirmation email?"** prompt with an actionable **Send** button. It only reveals "confirmation already sent for this booking" *after* you click — so the button reads as a pending action when the action already happened (a mild names-that-lie). The idempotency guard correctly blocks a real double-send, so it's cosmetic. Suggest the card reflect the already-sent state up front when the agent sent it autonomously. Iron-rule-4b: no test (redundancy/taste). Verified live 2026-07-23 (real Gmail delivery confirmed). | 🟡 |
| — | /login | Positive: clean, on-brand (Newsreader serif + mono labels + vermillion), password show/hide, "recover with a phrase" affordance present. | ✅ |

## Detail
<!-- ### UX-1 · <surface> — <one-liner>  (screenshot: <file>)
- what I saw / expected:
- why it matters:
-->
| UX-25 | visitor chat (answer body) | A grounded English answer carries an untranslated Chinese fragment mid-sentence: "it's not just a项目管理 trick" (round 2026-08-07T0027, gate-theory question on the real vault). The vault is bilingual, so the model code-switches on a term it read in Chinese; the visitor reads the owner's voice dropping a word they may not be able to read. Not a defect in the surface — the renderer shows what the model wrote — but it is what a recruiter sees. Worth a line in the visitor system prompt (answer in the language the visitor asked in, translate terms the corpus only has in another language) rather than a test. Iron-rule-4b: no test — freezing model phrasing as a contract would be brittle. | 💡 recorded |
| UX-26 | /admin/conversations (transcript overlay) | **Escape does not close the transcript overlay.** Round 2026-08-07T0027: opened a session's transcript, pressed Escape - the overlay stays, and because it covers the shell it swallows every click aimed at the sidebar (Playwright reported the transcript paragraph intercepting pointer events on the nav). Only the CLOSE ✕ control dismisses it. Escape-to-dismiss is the one modal affordance every user tries first; without it the overlay reads as stuck. Iron-rule-4b borderline - this one is behavioural rather than taste, so it can carry a test if the owner wants it. | 🟠 recorded |
| UX-27 | admin shell (sidebar footer, every page) | The rail's footer reads `instance · standmeet` / `uptime · —`. The uptime line is labelled on every admin page and renders an em-dash - a metric slot that never gets a value. Either wire it or drop the label; a permanent placeholder trains the owner to stop reading that corner. **ATTRIBUTED:** `AdminSidebar.tsx:169` renders `{t('uptimeLabel')} <span>—</span>` - the dash is a LITERAL in the JSX, not a fallback for a missing value. `SystemSection.tsx:49` renders `{d.uptime}` from the real system-info endpoint two components over. Frame shipped, wire missing - the same shape as F-E-2 (TOP MATCH renders unconditionally) and F-D-2 (the quota fill is a literal `w-0`). Round 2026-08-07T0027. Iron-rule-4b: no test on its own, but the CLASS deserves one - a gate that flags a labelled slot whose value is a literal placeholder. **③🧪 RED / ④🟩 GREEN (2026-08-07):** `admin-sidebar.spec.ts` asserts the footer's uptime matches, character for character, what `/admin/system` shows — asserting only "both look like a duration" would pass with two independently-computed values. RED as `sidebar-instance` not found. Both lines now read real sources: the handle from `useAdminSession`, the uptime from the same `useSystemInfo` store the system page uses, so they cannot drift. The OTHER line was the same defect unrecorded: `instance · standmeet` came from the i18n constant `sidebar.instanceName`, which names every instance ever deployed and therefore identifies none — key deleted. **⑤🙌 RE-VERIFIED (2026-08-07, prod):** the footer reads `instance · sijie` / `uptime · 8m 38s`, and /admin/system showed `uptime 8m 38s` in the same minute. | ✅ fixed |
| UX-28 | /admin/output (empty state) | The empty state says "Use **+ new output**, promote from /admin/wiki, or call MCP `promote_wiki_to_output`" - but no control on the page is called "+ new output". The two buttons read `+ PDF LEAD-MAGNET` and `+ WEB ESSAY`. The instruction names a button that does not exist by that name, so an owner following it looks for something that is not there. Round 2026-08-07T0027. Iron-rule-4b: no test (copy). | 🟡 recorded |
| UX-29 | admin nav · page vs pages | **Two concepts, three names, one letter apart.** Sidebar `custom pages` (CORPUS section) opens `/admin/custom-pages` titled **pages**; sidebar `landing page` (SETTINGS section) opens `/admin/page` titled **page**. So the owner navigates between a page called "page" and a page called "pages" that are unrelated - the public landing blocks vs MCP-built `/p/<slug>` documents. Round 2026-08-07T0027; this is what the nav-page-vs-pages item exists to judge. Suggest naming them by what they are (e.g. `landing` and `custom pages`) and making the heading match the nav label. Iron-rule-4b: no test - naming is the owner's call. | 🟡 recorded |
| UX-30 | /admin/skills (my skills) | **An installed skill does not say where it came from.** Round 2026-08-07T0027, driving `marketplace` check 2: installed `Brand Guidelines` live from the real anthropics/skills repo. It lands correctly (`8 tracked` → `9`, and the API confirms `source: marketplace`), but its card shows **only the name** — while every builtin card carries a `BUILTIN` badge. So on this instance `brand-guidelines` (from GitHub), `booking-enabler` (hand-pasted) and `prod-hand-pasted` (also hand-pasted) are visually indistinguishable, and nothing says the first one tracks an upstream that can move. The product HAS the field — the list endpoint returns `source` for every row, and the marketplace cards themselves show a `github` / `skillsmp` badge — it is simply not rendered on the installed side. Same shape as this round's convergence findings: the data exists on one face and is dropped on the other. Suggest the installed card carry the same source badge the marketplace card does. Iron-rule-4b: worth a test once the badge exists (assert an installed marketplace skill renders its provenance). | 🟡 recorded |
