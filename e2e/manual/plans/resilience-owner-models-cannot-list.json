{
  "_why": "resilience check 3 grid 2 —— 一个**能聊但列不出模型**的 provider。llm-fault 把 /models 钉成 403、聊天原样转发。走 owner 的 provider 卡（`/admin/api-mcp` 的 OWNER'S AI —— account 页指的就是这里；访客 BYOAI 那条路会被 SSRF 判据挡住，理由见 trajectory）。**endpoint 必须先保存**，否则后端仍用旧地址、代理一条都收不到（上一趟就栽在这儿）。驱完用 resilience-owner-restore.json 改回去。",
  "out": "../e2e/manual-runs/2026-08-07T0027/trajectory/resilience/shots",
  "viewport": [1280, 1400],
  "shots": [
    {
      "name": "res-13-endpoint-saved-to-proxy",
      "url": "/admin/api-mcp",
      "wait": 4000,
      "steps": [
        { "type": ["[data-testid=ai-provider-endpoint]", "http://llm-fault:9500"] },
        { "click": "[data-testid=ai-provider-save]" },
        { "wait": 3000 }
      ]
    },
    {
      "name": "res-14-cannot-list-says-so",
      "wait": 300,
      "steps": [
        { "typeSecret": ["[data-testid=ai-provider-key]", "EVAL_KEY"] },
        { "wait": 600 },
        { "click": "[data-testid=ai-provider-load-models]" },
        { "wait": 9000 }
      ]
    }
  ]
}
