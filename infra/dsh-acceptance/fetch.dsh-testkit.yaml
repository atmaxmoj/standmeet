schemaVersion: 1
name: standmeet-fetch-mcp-quick
subject:
  source: ../plugins/fetch
dsh:
  version: 0.1.1-rc.2
expect:
  boot: success
  rows:
    - fetch
  services: []
  tools:
    - mcp__fetch__fetch
# No exercise: the fetch tool makes a live outbound HTTP request. Registering the tool on real dsh is
# what this gate proves; fetching a URL needs network egress (a separate integration), so we do NOT do
# it here. Now JS (no Linux-musl wheels), so this runs on the local runner like every other block.
