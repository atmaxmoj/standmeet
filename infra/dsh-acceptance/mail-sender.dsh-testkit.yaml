schemaVersion: 1
name: standmeet-mail-sender-mcp-quick
subject:
  source: ../plugins/mail-sender
dsh:
  version: 0.1.1-rc.2
expect:
  boot: success
  rows:
    - mail-sender
  services: []
  tools:
    - mcp__mail-sender__send_email
# No exercise: send_email reaches back to the StandMeet host over STANDMEET_HOST_SOCKET to do the
# actual SMTP send. Registering the tool on real dsh is what this gate proves; sending needs a live
# host (a separate integration), so we do NOT fake one here.
