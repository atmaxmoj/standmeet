schemaVersion: 1
name: standmeet-summarize-mcp-quick
subject:
  source: ../plugins/summarize
dsh:
  version: 0.1.1-rc.2
expect:
  boot: success
  rows:
    - summarize
  services: []
  tools:
    - mcp__summarize__summarize_conversation
# No exercise: summarize_conversation has the StandMeet host run the LLM generation over
# STANDMEET_HOST_SOCKET. Registering the tool on real dsh is what this gate proves; generating a
# summary needs a live host (a separate integration), so we do NOT fake one here.
