# check-boundary-thin baseline —— boundaries that still hold a shape or a rule of
# their own. Shrink-only: move one resource into its domain, delete its line here.
# Never add a line — adding one means a boundary grew a second version of
# something the domain already says.
internal/security/facade/facade.go
internal/security/facade/facade_captcha.go
