# go build output (binary name = module directory name). Sandbox plugins are built
# from source into infra/plugins/<id>/ by infra/plugins/provision.sh (that directory
# is also ignored) — nothing reads this one here, don't commit it.
/mail-sender
