# go build output (binary name = module directory name). Sandbox plugins are compiled from source
# into infra/plugins/<id>/ by infra/plugins/provision.sh (that directory is also ignored); nobody
# reads this copy here — don't commit it.
/summarize
