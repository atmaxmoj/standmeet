# eval-harness LLM provider keys —— scenario YAML 的 `model:` 字段决定走
# 哪家；下面按 provider 配 API key (任一缺失启动报错并提示)。
#
# 复制成 `.env` 后填值；`.env` 已 gitignored。
#
# DeepSeek (默认)
DEEPSEEK_API_KEY=

# Anthropic Claude
ANTHROPIC_API_KEY=

# OpenAI
OPENAI_API_KEY=

# Google Gemini
GOOGLE_API_KEY=
