#!/usr/bin/env node
import('../dist/index.js').then((m) => m.main()).catch((err) => {
  process.stderr.write(`standmeet-mcp: ${err instanceof Error ? err.stack ?? err.message : String(err)}\n`);
  process.exit(1);
});
